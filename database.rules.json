{
  "rules": {
    "content": {
      ".read": true,
      ".write": false
    },
    "contentPending": {
      ".read": "auth != null && (auth.token.role == 'administrador' || auth.token.role == 'editor')",
      ".write": false
    }
  }
}
